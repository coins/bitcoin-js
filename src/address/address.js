import '../../../elliptic-js/src/secp256k1/secp256k1.js'

/**
 * Generates a P2PKH address
 * P2PKH means "pay to public key hash" 
 * @param {BigInt} privateKey 
 * @return {String} p2pkhAddress 
 */
export function privateKeyToP2PKH(privateKey) {
	return 32
}